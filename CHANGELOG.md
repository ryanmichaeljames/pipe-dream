# Changelog

## v1.1.0
- Update `Get-DataverseAuthToken` function to return expected token object property names.
- Add `New-DataverseBatchRequest` function that creates a request object for use with Invoke-DataverseBatchRequest.
- Add `Invoke-DataverseBatchRequest` function that executes a batch operation against the Dataverse Web API.
- Improve `README.md`

## v1.0.0
- Initial setup and configuration for PipeDream project
- Add `Get-DataverseAuthToken` function to get authentication token for Dataverse

## v0.0.0
- Nothing here
