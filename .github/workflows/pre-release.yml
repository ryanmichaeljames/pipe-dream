name: Release PipeDream Module

on:
  push:
    tags:
      - 'v*-alpha'

jobs:
  build:
    runs-on: windows-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Package module
      run: echo ${{ github.ref }}
